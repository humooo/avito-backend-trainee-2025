# Сервис назначения ревьюеров (Avito Tech Trainee Task)

Решение тестового задания для осенней стажировки 2025.

Сервис позволяет управлять пользователями, командами и автоматически назначать ревьюеров на Pull Request'ы согласно правилам (исключение автора, активность и т.д.).

## 🛠 Стек технологий

* **Go 1.23**
* **PostgreSQL 15** (pgx/v5 + pgxpool)
* **Chi** (HTTP роутинг)
* **Docker & Docker Compose**

##  Запуск

Всё завернуто в docker-compose, поднимается одной командой. База данных создается автоматически, миграции накатываются при старте сервиса.

```bash
docker-compose up --build
```

Сервис будет доступен на порту `:8080`.

### Тестирование
Написал bash-скрипт `test.sh` для **E2E (End-to-End) тестирования** основных сценариев (создание команд, PR, проверка автоназначения, мерж, идемпотентность).

Для запуска (при поднятом контейнере):
```bash
chmod +x test.sh
./test.sh
```

## История разработки и ход мыслей

В процессе работы над заданием проект прошел несколько этапов эволюции:

1.  **MVP (In-Memory):**
    Изначально реализовал слой хранения на Go-мапах, чтобы быстро отладить бизнес-логику назначения и статусов без настройки БД.

2.  **Миграция на PostgreSQL:**
    Заменил in-memory реализацию на **PostgreSQL**. Для работы с БД выбрал драйвер `pgx` (через `pgxpool`), так как он производительнее стандартного `database/sql`.

3.  **Рефакторинг ID (Int -> String):**
    Изначально использовал `int64`. Однако при детальном разборе `openapi.yaml` заметил, что примеры ID строковые (`"u1"`, `"pr-1001"`). Переписал модели и миграции на `TEXT`, чтобы строго соответствовать контракту.

4.  **Оптимизация назначения (Go -> SQL):**
    Логику выбора случайного ревьювера перенес в SQL (`ORDER BY RANDOM() LIMIT 2`).
    *Почему:* Это атомарно и защищает от Race Condition (состояние гонки). Если выбирать в памяти приложения, то между чтением списка и записью статус пользователя мог измениться.

## Особенности реализации

*   **Миграции:** Накатываются (`init.sql`) прямо в `main.go` при старте (упрощение для тестового). В проде лучше использовать `goose`.
*   **Архитектура:** Clean Architecture (`Handler` -> `Service` -> `Repository`).
*   **Валидация:** Используются сгенерированные через `oapi-codegen` структуры.

##  Дополнительные задания

Выполнены 4 из 5 бонусных задач:

*   [x] **Эндпоинт статистики:** `GET /stats` — возвращает количество ревью по пользователям.
*   [x] **Интеграционное тестирование:** Реализован сценарий `test.sh`.
*   [x] **Нагрузочное тестирование:** Проведен тест (Apache Benchmark). Результаты в файле `LOAD_TEST.md` (RPS ~2100).
*   [x] **Линтер:** Настроен `.golangci.yml` (проходит проверки `govet`, `staticcheck`, `errcheck`).

---
*Автор решения: Хабиб Боймуродов*
